# Model + bulk artifacts — not versioned.
# (Comments must be on their own line — inline comments are treated as part of the pattern.)
# data/raw/ = cached API responses + raw corpora (regenerable, large).
data/raw/
data/*.kuzu/
*.kuzu
claim_classifier*.pt

# Note: data/processed/ jsonl/json ARE versioned (expensive to regenerate: S2 sweeps,
# LLM typing).

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
