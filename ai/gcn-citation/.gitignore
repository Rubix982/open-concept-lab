# Python bytecode and caches
__pycache__/
*.py[cod]
*.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Local experiment outputs — ignore data contents but not curated ground truth
data/*
!data/ground_truth/
data/ground_truth/*
!data/ground_truth/cross_disciplinary_pairs.json
artifacts/.cache/
artifacts/.mplconfig/
artifacts/**/*.npy
artifacts/**/*.wal
artifacts/**/*.duckdb

# Notebook and OS noise
.DS_Store
