# TYPE    DATABASE        USER            ADDRESS                 METHOD

# Allow local UNIX socket connections (for psql, tools)
local     all             all                                     scram-sha-256

# Allow local connections over TCP (localhost only)
host      all             all             127.0.0.1/32            scram-sha-256
host      all             all             ::1/128                 scram-sha-256

# (Optional) Allow internal Docker/VM traffic on specific subnet (e.g., internal dev network)
# host    all             all             172.16.0.0/12           scram-sha-256

# (Optional) Add a specific external IP or subnet for admin access
# host    all             admin_user      203.0.113.42/32         scram-sha-256

# Reject everything else (paranoia mode — can be removed for dev)
host      all             all             0.0.0.0/0               reject
